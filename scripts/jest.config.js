/** Jest config for repository maintenance scripts (Node-only, no DOM). */
module.exports = {
    displayName: 'scripts',
    testEnvironment: 'node',
    testMatch: ['<rootDir>/__tests__/**/*.test.js'],
};
