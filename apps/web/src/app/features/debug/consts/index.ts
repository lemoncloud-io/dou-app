export const DEBUG_STORAGE_KEY = 'chatic-debug-mode';

/** Registry row that decides panel visibility by build stage (ADR-0080 결정 4). */
export const DEBUG_OVERLAY_KEY = 'debug.overlayEnabled';

export const DEBUG_CODE_MAX_ATTEMPTS = 3;
