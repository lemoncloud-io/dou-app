# Desktop Shell (Electron) runtime config.
# URL of the deployed Desktop Web client the shell loads (ADR-0001 remote load).
# Dev defaults to the local desktop-web vite server (http://localhost:5005) when unset.
VITE_DESKTOP_WEB_URL=https://desktop.chatic.example/index.html
