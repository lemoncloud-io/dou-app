{
    "name": "chatic-deferred-link-cleanup",
    "version": "1.0.0",
    "description": "Firebase Cloud Functions for cleaning up expired deferred deep links",
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "deploy": "npm run build && firebase deploy --only functions",
        "logs": "firebase functions:log"
    },
    "engines": {
        "node": "18"
    },
    "dependencies": {
        "firebase-admin": "^12.0.0",
        "firebase-functions": "^5.0.0"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}
