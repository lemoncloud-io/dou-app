/**
 * Shared bottom floating-panel surface — the rounded-top white bar with an
 * upward shadow used by FloatingButton and InlineActionButton. Kept in one place
 * so the panel padding + shadow stay identical across the two CTA presets.
 */
export const FLOATING_PANEL =
    'w-full rounded-t-[16px] bg-surface px-4 pb-4 pt-5 shadow-[0px_-10px_40px_0px_rgba(0,0,0,0.12)]';
